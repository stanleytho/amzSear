this needs to run with python3 
first setup the env
   python3 -m pip install --user virtualenv
   python3 -m virtualenv env
   source env/bin/activate
   pip install -r requirements.txt

run with 
python cli.py  -p 2 -i 35 --output json 'iphone'